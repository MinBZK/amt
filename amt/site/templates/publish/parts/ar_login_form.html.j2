{% if login_error is defined and login_error %}
    <c-alert kind="error" class="rvo-layout-margin-vertical--xl">
    <c-p class="rvo-layout-margin-vertical--sm">Er kon geen verbinding worden gemaakt met het Algoritmeregister.
    </c-p>
    <c-p>Controleer de ingevulde gegevens en probeer het opnieuw.</c-p>
    </c-alert>
{% endif %}
<c-card outline="true">
<c-alert style="margin-top: 0;" kind="warning" heading="Verbinding algoritmeregister" class="rvo-layout-margin-vertical--xl">
<c-p>Het algoritmeregister is geen onderdeel van AMT en om een algoritme te kunnen aanleveren of de status te
controleren, is aanmelden bij het
register noodzakelijk.
<c-link target="_blank" href="https://algoritmes.overheid.nl/nl/footer/registreer-je-algoritme">Lees hier
meer
over het aanmelden
</c-link>
</c-p>
</c-alert>
<c-form-fieldset>
<form id="ar-login-container"
      hx-ext="json-enc"
      hx-post="./login"
      hx-target="#publish-content-container"
      hx-swap="innerHTML"
      autocomplete="off">
    <div style="text-align: right;">
        <img src="/static/images/logo_register.png"
             style="mix-blend-mode: multiply" />
    </div>
    <c-text-input-field id="username" name="username" label="{% trans %}Name{% endtrans %}" required="true"
    {% if values is defined and values.get('username') %}value="{{ values.get('username') }}"{% endif %}
    {% if errors is defined and errors.get('username') %}errorText="{{ errors.get('username') }}"{% endif %}
    />
    <c-text-input-field type="password" id="password" name="password" label="{% trans %}Wachtwoord{% endtrans %}" required="true"
    {% if errors is defined and errors.get('password') %}errorText="{{ errors.get('password') }}"{% endif %}
    />
    <c-button type="submit" hx-disabled-elt="this">
    Verbinding maken
    </c-button>
</form>
</c-form-fieldset>
</c-card>
